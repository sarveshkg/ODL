apiVersion: v1
kind: Service
metadata:
  labels:
    app: odl-system
  name: odl-system-service
spec:
  ports:
  - port: 80 #change to 80
    protocol: TCP
    targetPort: 80 #change to 80
    nodePort: 30333
  selector:
    app: odl-system
  type: NodePort
